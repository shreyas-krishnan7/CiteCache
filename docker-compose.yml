version: "3.9"

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: citecache-qdrant
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC API
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage:
